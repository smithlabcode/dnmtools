#!/usr/bin/env bash

infile=tests/reads.sam
if [[ -e "${infile}" ]]; then
    ./dnmtools format -f abismal ${infile} tests/test.fmt.bam
else
    echo "${infile} not found; skipping remaining tests";
    exit 77;
fi
