#!/usr/bin/env bash

inputfile=test.fa
if [[ -e "${inputfile}" ]]; then
    ./dnmtools simreads -o test -n 10000 -m 0.01 -b 0.98 ${inputfile};
else
    echo "${inputfile} missing; skipping remaining tests";
    exit 77;
fi
