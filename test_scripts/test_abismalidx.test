#!/usr/bin/env bash

inputfile=tests/tRex1.fa
if [[ -e "${inputfile}" ]]; then
    ./dnmtools abismalidx ${inputfile} tests/tRex1.idx
else
    echo "${inputfile} not found; skipping remaining tests";
    exit 77;
fi
